# Testing if this workflow runs when i trigger it manually.
name: Release

on:
  workflow_dispatch:

jobs:
    # test:
    #     runs-on: ubuntu-latest
    #     steps:
    #       - uses: actions/checkout@v4
    #       - name: Setup .NET
    #         uses: actions/setup-dotnet@v4
    #         with:
    #             dotnet-version: 10.x
    #       - name: Restore dependencies
    #         run: dotnet restore
    #       - name: Build
    #         run: dotnet build --no-restore
    #       - name: Test
    #         run: dotnet test --no-build --verbosity normal
    
    build-package:
        runs-on: ubuntu-latest
        permissions:
            contents: read
        steps:
          - uses: actions/checkout@v2
          - name: Setup .NET
            uses: actions/setup-dotnet@v4
            with:
                dotnet-version: 10.x
          - name: Pack
            run: dotnet pack OpenCompote
          - name: Get Version
            id: get-version
            run: |
              VERSION=(dotnet msbuild OpenCompote -getProperty:Version)
              echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
          - name: Upload artefact
            uses: actions/upload-artifact@v4
            with:
                name: OpenCompote-v${{steps.get-version.outputs.VERSION}}
                path: ./OpenCompote/bin/Release/*.nupkg
